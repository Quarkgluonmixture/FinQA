1.首先我进行了根据experiment_matrix.md，我负责全部模型训练（4B / 8B SFT + 数据量消融） 
•	Pipeline 验证：确认 debug/small/full 配置均可跑通
•	评测执行：训练后立即跑评测，确认输出格式与 math-verify 兼容


2.然后我实施了计划：# 修订对齐计划（含 Percent/格式/采样/Fallback）

## Summary
- 维持双分支主设定：`answer_only` vs `formula_rationale`，推理固定 `--no-enable_thinking`。
- 新增并锁死 5 个关键规格：`percent scale` 数据自洽、多步公式一行展开、LoRA tokenizer 兼容校验、分层嵌套采样、`error shift` 必做。
- 执行顺序：Debug 验证 -> 4 个 full 主实验 -> 8B winner-style 分层消融 -> 强制 error-shift 分析。

## 关键规格（全部强制）
1. `percent scale` 训练数据规范（在 [build_formula_rationale_targets.py](/home/jiaming/workspace/FinQA/stage1/scripts/build_formula_rationale_targets.py)）
- 对每条样本执行 program 得到 `formula_value`。
- 解析原始 `gold_value`，记录关系标签：`consistent`、`x100_match`、`div100_match`、`mismatch`。
- 只要不一致，训练 target 的最终答案一律改为 `formula_value`（保留 `gold_raw` 供审计）。
- 输出审计字段：`gold_raw`、`gold_value`、`formula_value`、`scale_relation`、`answer_corrected`。

2. `formula_rationale` 多步格式锁死（采用你建议的选项 2）
- 统一格式：
  - `Formula: <单行内联表达式>`
  - `[FINAL_ANSWER]<number>[/FINAL_ANSWER]`
- 多步 program 的 `#k` 必须内联展开成最终一行表达式，不输出中间步骤文本，不输出长 CoT。

3. LoRA 评测加载与 tokenizer 兼容（在 [eval_finqa.py](/home/jiaming/workspace/FinQA/finqa_baseline/eval_finqa.py)）
- 新增 `--adapter_path`，加载顺序为 `base model + adapter`。
- tokenizer 默认优先从 `adapter_path` 加载；若无 tokenizer 文件才回退 base tokenizer。
- debug 阶段强制做一次兼容检查：同一输入下，`base` 与 `base+adapter` 都能稳定产出可解析 `FINAL_ANSWER` 标签。

4. Stage B 子集策略（在 [run_train_eval_matrix.sh](/home/jiaming/workspace/FinQA/stage1/scripts/run_train_eval_matrix.sh) 驱动）
- 固定 `250 ⊂ 1000 ⊂ full`（嵌套子集）。
- 按 `program_steps` 分层采样，保持单步/双步/多步比例近似 full。
- 固定随机种子并落盘样本 ID 清单，保证可复现。

5. Fallback 故事前置
- 若两种 supervision 在总分接近（例如差异不显著），主结论转向 error composition 差异，而不是仅看 acc。
- 因此 `error shift analysis` 从可选改为必须项。

## 实验执行
- Debug 必跑 4 个：`4B/8B × answer_only/formula_rationale`。
- Full 主实验必跑 4 个：`4B/8B × answer_only/formula_rationale`。
- Decision Gate：选出 `8B winner_style`。
- Stage B：
  - 8B 必跑：`0/250/1000/full`（winner_style）。
  - 4B 最低要求：`0/full`（winner_style 对齐锚点）。
- 评测参数锁定：`oracle + no-thinking + final_answer_tag + math_verify`。

## Test Plan（强制验收）
- 格式回归：覆盖 `answer_only` 与 `formula_rationale`（含 open/absent tag、含 formula 行）。
- 数据自洽回归：统计 `scale_relation` 分布与 `answer_corrected` 数量，抽样人工复核。
- 采样回归：验证嵌套关系与分层比例偏差阈值。
- 运行回归：每个 run 必须产出 checkpoint、`finqa_*.jsonl`、`summary.json`。
- 分析回归：必须交付 pre/post error taxonomy 与两种 supervision 的 error-shift 对比表。

## Assumptions
- `formula_rationale` 只允许短公式监督，不引入长自然语言 CoT。
- percent 不一致时以公式计算结果为准（训练目标内部自洽优先）。
- 8B 是消融主角，4B 作为 matched anchor。


3.再然后，我已生成详细实验报告文件：

sft_ablation_detailed_report_2026-04-04.md

4.再然后是这些情况：第一个问题：训练数据量是 25185，不是 6200
你的原始计划里 "full" 是 FinQA train split 约 6200 条。但报告显示训练数据有 25185 行，而且 mismatch 率达到 32%（8196 条数据的 formula 执行结果与 gold answer 不一致）。
这意味着大约三分之一的训练样本在教模型错误的东西。4B 的灾难性退化很可能和这个直接相关——模型在大量自相矛盾的数据上训练，把原来对的能力也覆盖掉了。
你需要立刻确认：这 25185 条是只有 FinQA 还是混入了 ConvFinQA / MultiHiertt？如果混入了，那数据清洗没做好是最大的问题源。



上 3 条消息
你说得对，我已经确认了，结论很明确：

25185 不是纯 FinQA，是混合数据。
train.jsonl 组成是：
convfinqa: 11104
finqa: 6251
multihiertt: 7830
所以你原计划里的“FinQA full ~6200”其实对应的是这 6251 条。
证据：

合并逻辑在 merge_datasets.py：processed/*_train.jsonl -> unified/train.jsonl
数据格式文档也写明是三库合并 README_DATA.md
实际文件是 train.jsonl
另外你提到 mismatch 风险也成立，但补充一点：

mismatch=8196 里有 6318 条已被改成 formula_value 作为训练 target（不是继续用原 gold）。
仍有约 1878 条没改掉，主要是公式执行失败/答案解析失败导致，风险依然很大（尤其混入 MultiHiertt 后）。
如果你同意，我下一步直接做两件事：

生成 finqa-only 的 train/dev/debug
把矩阵脚本切到 finqa-only 默认，再跑一轮 4B/8B 快速验证（先 debug）

数据问题已经定位清楚了，修复方案很直接。

## 修复策略：分两轮跑

### 第一轮（最高优先级）：只用 FinQA clean 子集

这是你最可控的实验。只保留 FinQA 的 6251 条中 consistent 的样本，完全排除 ConvFinQA 和 MultiHiertt。

**数据过滤逻辑：**

python
# 伪代码，你需要适配到你的实际字段名
import json

clean = []
for line in open("train.jsonl"):
    sample = json.loads(line)
    
    # 条件1：只保留 FinQA
    if sample["source"] != "finqa":
        continue
    
    # 条件2：只保留 formula 执行成功且与 answer 一致的
    if sample.get("scale_relation") != "consistent":
        continue
    
    # 条件3：如果被修正过，确认修正后的 target 可用
    # （这一步取决于你的字段结构，核心是确保训练 target 和 formula 计算结果一致）
    
    clean.append(sample)

# 预期：6251 × 0.93 × (1 - 少量剩余 mismatch) ≈ 4500-5000 条


跑完之后你需要确认三件事：clean 子集有多少条、单步/双步/多步的分布、以及抽查 20 条确认 target 和 formula 确实一致。

**从这个 clean set 切出 ablation 子集：**

debug:  50   (pipeline 验证用)
small:  250  (ablation)
medium: 1000 (ablation)
full:   全部 (~4500-5000)


嵌套关系和分层采样策略不变。

### 第二轮（如果第一轮结果好且有余力）：加入 ConvFinQA clean

如果 FinQA-only clean 的结果确认了"clean data → SFT 有效"，你可以再跑一组加入 ConvFinQA consistent 样本的实验，看额外数据是否进一步帮助。这个对比本身就是一个 finding：同域 clean data vs 跨域 noisy data 的效果差异。

但 MultiHiertt 建议完全排除。它的表格结构和 FinQA 差异太大，formula 执行失败率高，是噪声的主要来源。

## 4B 的超参必须调

4B 在脏数据 full set 上暴跌 18pp，不完全是数据的锅——数据干净了如果超参还是太激进，依然会过拟合。建议做以下调整：

| 参数 | 原值（推测） | 建议值 | 原因 |
|---|---|---|---|
| learning_rate | 2e-4 | **5e-5** | 4B 参数少，2e-4 太激进 |
| num_epochs | 3 | **2** | 减少过拟合风险 |
| LoRA rank | 16 | 16（不变） | 先保持，如果还退化再降到 8 |
| max_seq_length | 看现有值 | 确认 ≤ 1024 | 太长会导致有效 batch 下降 |

8B 的超参可以保持不变，因为它在脏数据上都没退化。

## 具体要跑的 runs

**必跑（6 个 runs）：**

| run | 说明 |
|---|---|
| 4b_finqa_clean_full_answer_only | 核心实验：4B 在干净数据上是否还退化 |
| 8b_finqa_clean_full_answer_only | 核心实验：8B 在干净数据上是否进一步提升 |
| 4b_finqa_clean_full_formula_rationale | 对照：supervision style 在干净数据上有无差异 |
| 8b_finqa_clean_full_formula_rationale | 对照 |
| 8b_finqa_clean_250_answer_only | ablation 低点 |
| 8b_finqa_clean_1000_answer_only | ablation 中点 |

Zero-shot baseline 不需要重跑——你已经有了，但**注意你报告里的 zero-shot 数字和之前的不一样**：

| 来源 | 4B oracle base | 4B oracle adjusted |
|---|---|---|
| 之前的 final_report.md | 16.57% | 25.81% |
| 这次报告的 4b_zeroshot | 19.6% | 31.4% |

这个差异必须搞清楚。可能是 evaluator 版本不同、prompt 不同、或者数据 split 不同。如果你发现差异来源是 evaluator 更新了，那所有对比都应该用这次的 zero-shot 数字。如果是 prompt 变了，那你需要决定用哪个版本作为最终 baseline。

## 修改后的时间线

| 日期 | 任务 |
|---|---|
| 4/4 (今天) | 数据过滤脚本 + 生成 clean JSONL + 确认 zero-shot 数字差异 |
| 4/5 | debug run 验证 pipeline（4 个 debug，确认不炸） |
| 4/6-4/7 | 跑 6 个必跑 runs |
| 4/8 | 结果分析 + error shift |
| 4/9-4/10 | 如果结果好，跑第二轮（+ConvFinQA clean） |
| 4/11-4/16 | 写报告 |
| 4/17 | 提交 |

你现在最紧急的事是写那个数据过滤脚本，然后确认 zero-shot 数字差异的原因。这两件事搞完，告诉我结果，我帮你判断后续要不要调整方案。



上 2 条消息
两件事已完成，结果如下。

FinQA-clean 过滤脚本已落地并跑完
脚本：
build_finqa_clean_splits.py
严格按你第一轮策略（source=finqa + formula_exec_ok + scale_relation=consistent）产物：

目录：stage1/data/finqa_clean
摘要：clean_summary.json
关键统计：

train: 3277（从 25185 过滤后）
dev: 475
train 分布：single 1962 / double 873 / multi 442
debug(50): single 30 / double 13 / multi 7
250/1000 子集：已生成且 250 ⊂ 1000 为 true
抽检 20 条：target_answer 与 formula_value 一致，失败 0
补充：我也做了一个可选版本（consistent OR corrected，仍要求 formula_exec_ok），数量是 train 5933（更接近你预期量级）。
目录：stage1/data/finqa_clean_consistent_or_corrected

zero-shot 数字差异根因已定位
诊断文档：
zero_shot_discrepancy_diagnosis_2026-04-04.md
结论：你比较的是两套不同协议/时代的结果，不可直接对比。

旧：results_recovered/a656988（历史恢复包，acc_base/acc_adjusted 体系，旧输出策略，且旧记录里有 max_new_tokens=32）
新：results/thinking_false（math_verify + final_answer_tag + max_new_tokens=256）
建议：后续 Stage1 全部统一用当前这套 baseline（thinking_false 家族）作为对照，不再混用 results_recovered/*。

下一步你拍板一个就行：

严格 consistent-only（train=3277）继续跑 6 个必跑 run。
用 consistent+corrected（train=5933）作为 clean-full，再跑 6 个必跑 run。
两者都跑一轮小规模 debug（50）再定。

用 **选项 1：strict consistent-only（3277）**，不需要先跑 debug 再定。理由很简单。

## 为什么选 3277 而不是 5933

5933 里多出来的 2656 条是 corrected 样本——原始 gold answer 和 formula 不一致，被脚本改成了 formula_value。这个修正本身引入了一层不确定性：如果 formula 解析错了，修正后的 target 也是错的。你上一轮实验已经用惨痛代价证明了脏数据对 4B 的破坏力。3277 条每一条都经过了 formula_exec_ok + consistent 双重验证，是你目前最干净的数据。

3277 对 LoRA SFT 来说完全够用。很多 reasoning 方向的工作用 1k-2k 就能看到显著效果。而且你的 ablation 设计是 250/1000/full，3277 作为 full 正好覆盖了一个合理的范围。

如果 3277 跑出来效果好，5933 可以作为后续 ablation 的一个额外数据点：clean-strict vs clean-relaxed，看多出来的 corrected 数据到底是帮忙还是添乱。这本身也是一个 finding。

## 确认最终对照 baseline

按你的诊断结论，统一用当前协议的 zero-shot 数字：

| 模型 | acc_base | acc_adjusted |
|---|---|---|
| 4B zero-shot | 19.62% | 31.39% |
| 8B zero-shot | 15.52% | 20.40% |

所有 SFT 结果都和这组数字比。旧的 results_recovered 不再引用。

## 4B 超参必须改

上一轮 4B 退化 18pp。数据干净了之后如果超参不调，仍然有过拟合风险。3277 条比上次的 25185 少了 87%，更容易过拟合。确认以下调整：

| 参数 | 上轮值 | 本轮值 | 原因 |
|---|---|---|---|
| learning_rate | 2e-4 | **5e-5** | 3277 条数据量小，必须降 |
| num_epochs | 3 | **2** | 减少过拟合 |
| 8B learning_rate | 2e-4 | **2e-4（不变）** | 8B 上轮没退化 |
| 8B num_epochs | 3 | **2** | 数据量变小，略降 |

## 最终 run list（6 个）

| # | run_id | model | data | style | 超参变化 |
|---|---|---|---|---|---|
| 1 | 4b_clean_full_answer_only | 4B | 3277 | answer_only | lr=5e-5, ep=2 |
| 2 | 4b_clean_full_formula_rationale | 4B | 3277 | formula_rationale | lr=5e-5, ep=2 |
| 3 | 8b_clean_full_answer_only | 8B | 3277 | answer_only | lr=2e-4, ep=2 |
| 4 | 8b_clean_full_formula_rationale | 8B | 3277 | formula_rationale | lr=2e-4, ep=2 |
| 5 | 8b_clean_250_answer_only | 8B | 250 | answer_only | lr=2e-4, ep=2 |
| 6 | 8b_clean_1000_answer_only | 8B | 1000 | answer_only | lr=2e-4, ep=2 |

Zero-shot 不重跑，直接用已有结果。评测协议不变：oracle + no-thinking + final_answer_tag + math_verify。

跑完之后把结果发给我，我帮你判断是否需要跑 5933 的 relaxed 版本，以及论文的故事线怎么组织。



上 10 条消息
已按你给的方案启动正式实验（strict consistent-only 3277，直接 6 runs，无 debug 决策阶段）。

主脚本：run_train_eval_matrix_clean_strict.sh
当前后台状态：main/watchdog/ntfy 都在运行，当前在 4b_clean_full_answer_only 训练阶段
我额外修正了一个关键点：full 训练强制 max_steps=-1，避免被默认 max_steps=100 截断
ntfy 站点是：

https://ntfy.sh/finqa-stage1-clean-strict
实时日志：

总日志：train_eval_clean_strict.log
后台守护日志：train_eval_clean_strict_background.log
当前 run 训练日志：train.log
我会在跑完 6 个 run 后，把结果（含 error-shift）整理给你。


验证下ntfy是否正常在跑



上 4 条消息
已验证并修复，ntfy 现在是正常在跑的。

当前状态：main/watchdog/ntfy 都是 running
main pid=1447343
ntfy pid=1474315
ntfy 站点：https://ntfy.sh/finqa-stage1-clean-strict
我刚发了一条健康检查消息，返回 HTTP 200，并拿到消息回执：
id=qQaVGfIJKYLs
event=message
topic=finqa-stage1-clean-strict
如果你现在打开这个 topic 页面，应该能看到这条 healthcheck 通知。


实验也正常是吧



上 1 条消息
是的，现在实验是正常在跑的。

主流程：running（pid=1447343）
watchdog：running
ntfy：running
当前进度：4b_clean_full_answer_only 已训练完成，正在评测（Evaluating FinQA，约到 302/1147）


5.之后遇到了情况：将这次的实验结果写成report



上 4 条消息
实验报告已写好，文件在这里：

sft_ablation_strict_consistent_report_2026-04-04.md
内容已包含本次 6 个 run 的结果表、与 zero-shot 对比、error-shift 结论、执行偏差（含 max_steps 不一致）和后续建议。
如果你要我，我可以再把这份报告合并覆盖到你当前的 sft_ablation_detailed_report_2026-04-04.md。


结果里有一个比 max_steps 不一致更严重的问题。
所有 SFT run 都退化了，包括 8B
上一轮脏数据实验中，8B full 的 base accuracy 从 15.52% 涨到了 17.87%。这一轮用干净数据，8B full base 反而只到 10.99%，比 zero-shot 还低 4.5pp。4B 继续暴跌。干净数据不但没修复问题，反而更差了。
这不正常。数据质量提升不应该导致性能下降。说明问题不在数据，而在训练或评测的某个环节有系统性 bug。
最大嫌疑：训练格式和推理格式不匹配
你的 SFT repo 原本是给 thinking=true 设计的。你的推理用的是 --no-enable_thinking。
如果训练数据的 output 字段长这样：
<think>
Step 1: ...
</think>
[FINAL_ANSWER]0.14[/FINAL_ANSWER]
但推理时 thinking 被关掉了，模型在 inference 时根本不会进入 <think> 模式。它被训练去生成一种它在推理时无法使用的格式。这会导致模型输出混乱，解释了为什么所有 run 都退化。
另一种可能：训练时 prompt 模板和评测时的 prompt 模板不一致。如果训练时用的 instruction 格式和 eval_finqa.py 里的 prompt 不同，模型在评测时看到的是一个从未见过的 prompt，输出自然不对。
你在跑任何新实验之前，必须先做以下诊断
第一步：对比训练样本和推理 prompt（10 分钟）
bash# 从训练数据里抽一条，看完整的 input + output
head -1 stage1/data/finqa_clean/train.jsonl | python -m json.tool

# 从评测 jsonl 里抽一条，看实际发给模型的 prompt
head -1 finqa_baseline/results/sft_clean_strict/8b_clean_full_answer_only/finqa_*.jsonl | python -m json.tool
对比这两条。具体检查：训练时的 system prompt / instruction 和评测时发给模型的 prompt 是否一致？训练 output 里有没有 <think> 标签？评测时模型的 raw_output 长什么样？
第二步：看一条 SFT 模型的 raw output（5 分钟）
从评测 jsonl 里找 5 条 raw_output，看模型实际在输出什么。如果你看到模型在试图输出 <think> 标签但被截断，或者输出一堆无关内容，那就是格式不匹配。如果模型输出的格式正确但答案算错了，那是另一个问题。
第三步：确认 LoRA adapter 确实被加载了（5 分钟）
对同一条输入，分别用 base model 和 base + adapter 跑一次，看输出是否不同。如果完全一样，说明 adapter 没加载成功。你上一轮的 compat check 通过了 4/4，但那是在脏数据上——这一轮需要重新确认。
为什么 max_steps 问题是次要的
4B 的两个 run——一个 max_steps=-1（跑满 2 个 epoch），一个 max_steps=100（不到 1/4 个 epoch）——退化幅度几乎一样（-15.26pp vs -14.30pp）。如果问题是过拟合，跑满 epoch 的应该明显更差。两个都一样差，说明问题出在第一步就错了（格式不匹配），而不是训得太多。
直接用脚本跑这三个



上 11 条消息
三个诊断脚本已经跑完，结论明确了，报告在：

diagnostic_prompt_adapter_8b_clean_full_answer_only_2026-04-05.md

关键结果：

训练格式 vs 推理格式
已确认存在协议不一致：训练是单串 Instruction/Input/Output，评测是 chat system+user。
<think> 标签不匹配不是主因（两边都没 <think>）。
5 条 raw output 检查
大部分能输出 [FINAL_ANSWER]...[/FINAL_ANSWER]。
但有明显异常（如 open_only + runaway digits），且有多条 scale 错误（例如 gold=0.14, pred=14.5）。
LoRA adapter 是否加载
同一批 10 条输入里，base 与 base+adapter 仅 2/10 完全相同。
说明 adapter 确实加载并生效，不是“没加载到”的问题。
结论：当前最可疑主因是训练/评测 prompt 协议错配，不是数据清洗本身、不是 <think> 开关、也不是 adapter 加载失败。

如果你同意，我下一步就直接改成“训练与评测完全同模板”再跑一个最小复现实验（8B clean 250）做快速验证。


最快的修复路径：只改评测，不重新训练
把 eval_finqa.py 的 prompt 改成和训练时完全一致的格式。具体来说：
python# 目前的 eval prompt（chat 格式，需要改掉）
# system: "You are a financial QA assistant..."
# user: "Context: ...\n\nQuestion: ...\n\nReturn exactly one..."

# 改成和训练一致的格式：
prompt = f"""Instruction:
Solve the financial numerical reasoning problem. Return exactly one tagged final answer as [FINAL_ANSWER]...[/FINAL_ANSWER] in the output.

Input:
Context:
{context}

Question:
{question}"""
然后你需要确认一件事：训练时这个 prompt 是作为单个 string 传给 tokenizer 的，还是被包在 chat template 里（比如 <|im_start|>user\n...<|im_end|>）？这取决于你 SFT repo 的 preprocessing 逻辑。如果训练时用了 chat template，评测时也必须用同样的 chat template。
检查方法：
bash# 看 SFT 代码里 tokenizer 是怎么处理训练样本的
grep -n "apply_chat_template\|tokenizer\|chat_template" stage1/scripts/prompting.py
grep -n "apply_chat_template\|tokenizer\|chat_template" stage1/train_sft.py
如果训练代码里用了 tokenizer.apply_chat_template()，你需要看它传入的 messages 结构是什么样的，然后在 eval 时完全复制这个结构。
执行步骤
步骤 1（30 分钟）：确认训练时的完整 tokenized prompt
从训练代码中找到 prompt 构建逻辑，打印出一条完整的训练样本经过 tokenizer 之后的文本（decode 回来看），确认模型实际看到的输入长什么样。
步骤 2（30 分钟）：修改 eval prompt
把 eval_finqa.py 的 prompt 构建逻辑改成和训练完全一致。如果训练用了 chat template，eval 也用同样的 chat template。如果训练是纯文本拼接，eval 也用纯文本拼接。
步骤 3（2 小时）：只重跑评测，不重新训练
你已有的 6 个 checkpoint 不需要重新训练。只需要用修正后的 eval prompt 重新跑一遍评测：
bash# 对已有的每个 checkpoint 重新评测
for run in 4b_clean_full_answer_only 4b_clean_full_formula_rationale \
           8b_clean_full_answer_only 8b_clean_full_formula_rationale \
           8b_clean_250_answer_only 8b_clean_1000_answer_only; do
    python eval_finqa.py \
        --model_name <base_model> \
        --adapter_path <checkpoint_path>/$run \
        --setting oracle \
        --no-enable_thinking \
        # ... 其他参数不变
done
步骤 4（重要）：也用新 prompt 重跑 zero-shot baseline
因为 prompt 变了，zero-shot 的数字也会变。你必须用完全相同的 prompt 跑一次无 adapter 的 zero-shot，作为新的对照。
用脚本后台实现以上内容

6.我实施了以下计划：# Prompt 对齐后“仅重评测”后台执行方案（不重训）

## Summary
- 目标：不重新训练，只通过评测端 prompt 对齐修复协议错配，并重跑 `6 个 SFT checkpoint + 2 个 zero-shot`。
- 已锁定决策：
  - `eval` 采用**新增开关**（保留旧行为）。
  - 结果写入**新目录隔离**，不覆盖旧结果。
- 后台运行复用现有 `watchdog + ntfy` 机制，新增专用 wrapper 脚本。

## Key Changes（实现级别）
1. `eval_finqa.py` 增加 prompt 协议开关并支持“训练同款输入”
- 新增 CLI：
  - `--prompt_protocol {chat_default,stage1_train_text}`（默认 `chat_default` 保持兼容）
- `stage1_train_text` 模式行为：
  - 构造与训练一致的单字符串输入（不走 `system+user`）：
    - `Instruction: ...`
    - `Input: Context... Question...`
  - 直接 `tokenizer(prompt_text, return_tensors="pt")`，不走 `apply_chat_template`
  - `--enable_thinking/--no-enable_thinking` 在该模式下不参与输入构造（仅保留参数兼容）
- `run_record` 中新增字段：`prompt_protocol`，保证报告可追溯。

2. `finqa_baseline/utils/prompting.py` 新增训练同款 prompt builder
- 新增函数（示意）：`build_finqa_trainstyle_prompt(...)`
- 复用现有 `oracle/full` 上下文抽取逻辑，仅替换外层模板为训练格式（不包含 `Output:` 段）。
- `final_answer_tag` 文案保持与训练模板一致。

3. 新增“仅评测”矩阵脚本（重跑 8 个 run）
- 新脚本：`stage1/scripts/run_eval_matrix_prompt_aligned.sh`
- 固定参数：`oracle + no-thinking + final_answer_tag + math_verify`
- 重跑列表：
  - `4b_clean_full_answer_only`
  - `4b_clean_full_formula_rationale`
  - `8b_clean_full_answer_only`
  - `8b_clean_full_formula_rationale`
  - `8b_clean_250_answer_only`
  - `8b_clean_1000_answer_only`
  - `4b_zeroshot_prompt_aligned`（无 adapter）
  - `8b_zeroshot_prompt_aligned`（无 adapter）
- 结果新目录（隔离）：
  - `finqa_baseline/results/sft_clean_strict_prompt_aligned_eval/`
- 产物：
  - 每个 run：`finqa_*.jsonl`、`summary.json`、`error_cases.md`
  - 总表：`run_manifest.jsonl`
  - `error_shift_report.md/json`（仅对 6 个 SFT run 做 style 对比）

4. 新增后台 wrapper（复用现有 watchdog/ntfy）
- 新脚本：`stage1/scripts/run_eval_prompt_aligned_background.sh`
- 内部设置并委托 `run_train_eval_background.sh`：
  - `RUN_SCRIPT=stage1/scripts/run_eval_matrix_prompt_aligned.sh`
  - 独立日志/PID 文件：
    - `stage1/logs/eval_prompt_aligned.log`
    - `stage1/logs/eval_prompt_aligned_background.log`
    - `eval_prompt_aligned_{main,watchdog,ntfy}.pid`
  - 默认 `NTFY_TOPIC=finqa-stage1-evalfix`（支持环境变量覆盖）
- 对外命令保持统一：`start|status|stop|restart`。

## Test Plan（验收标准）
1. Prompt 对齐验收
- 单样本打印训练文本与 `stage1_train_text` eval prompt 头部，确认模板字段一致（`Instruction/Input/Context/Question`）。
- 在 `stage1_train_text` 模式下，确认不走 `apply_chat_template` 分支。

2. 评测烟测
- 先对 `8b_clean_full_answer_only` 跑 `--num_samples 20`：
  - 产出 `summary.json/jsonl/error_cases.md`
  - `FINAL_ANSWER` 解析与 math-verify 正常，无崩溃。

3. 全量重评测
- 8 个 run 全部完成并落盘到新目录。
- `run_manifest.jsonl` 条目完整，字段含 `prompt_protocol=stage1_train_text`。

4. 后台与通知
- `status` 显示 `main/watchdog/ntfy` 均 running。
- 日志出现阶段推进；ntfy 至少收到 `Started` 和 `Completed` 通知。
- watchdog 在日志停滞超时后可发告警（可通过缩短超时做一次演练）。

## Assumptions
- 不修改训练代码与 checkpoint，仅改评测路径。
- 对外默认行为不破坏：未传 `--prompt_protocol` 时沿用旧 chat 流程。
- 新实验结论以“新 prompt 下重跑 zero-shot”作为统一基线；旧 `sft_clean_strict` 目录保留仅供对照。
